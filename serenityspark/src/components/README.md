# SerenitySpark Components

Place reusable React components for the SerenitySpark app in this folder.

- Use for UI elements or complex widgets that are shared between pages.
- Example: Button, Card, Modal, FloatingAction, Spinner, etc.

For now, create your components as individual `.js` files in this directory.
